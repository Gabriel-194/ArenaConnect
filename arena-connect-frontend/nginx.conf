server {
    listen 80;

    # Onde os arquivos do seu site vão ficar
    root /usr/share/nginx/html;
    index index.html index.htm;

    # A mágica acontece aqui:
    location / {
        try_files $uri $uri/ /index.html;
    }
}